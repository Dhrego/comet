---
const fonts = await Astro.glob("../fonts/*.woff2");
---

<>
  {
    fonts.map((font) => (
      <link
        rel="preload"
        as="font"
        href={font.default}
        type={`font/${font.default.split(".").pop()}`}
        crossorigin="anonymous"
      />
    ))
  }
</>
